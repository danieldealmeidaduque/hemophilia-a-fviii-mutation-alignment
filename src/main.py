from slice import Slice
from align import Align
from mutate import Mutate


if __name__ == "__main__":

    pass
    # mutate = Mutate("Human_FVIII_prot.fasta", "FVIII_point_mutations_v1_clean.csv")

    # aligne = Align()

    # slice = Slice()
